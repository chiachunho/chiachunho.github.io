#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Load nvm and use the project's .nvmrc Node version when available.
if [ -s "$HOME/.nvm/nvm.sh" ]; then
  . "$HOME/.nvm/nvm.sh"
  if [ -f ".nvmrc" ]; then
    nvm use || true
  fi
fi

npx lint-staged
